on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v3

      - name: "Login To Docker Hub"
        run: docker login --username basmamohamed --password dckr_pat_AyOUlk5zY9_0noFl-aXCgecLJko

    
